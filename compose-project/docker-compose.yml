version: "3"

services:

  web:
    image: nginx:latest
    container_name: web-nginx
    ports:
      - "8082:80"
    environment:
      APP_NAME: ${APP_NAME}

  cache:
    image: redis:latest
    container_name: redis-cache
    environment:
      REDIS_PORT: ${REDIS_PORT}
